[unstable]
build-std = ["core", "compiler_builtins", "alloc"]
build-std-features = ["compiler-builtins-mem"]

[target.x86_64-unknown-linux-stablemod]
rustflags = [
	"-C", "relocation-model=pic",
	"-C", "link-args=-static",
	"-C", "link-args=-pie",

]
[target.x86_64-unknown-none]
rustflags = [
	"-C", "relocation-model=pic",
	"-C", "link-args=-static",
	"-C", "link-args=-pie",
]
[target.aarch64-unknown-none]
rustflags = [
	"-C", "relocation-model=pic",
	"-C", "link-args=-static",
	"-C", "link-args=-pie",
]

