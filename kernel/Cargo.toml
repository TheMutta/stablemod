[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[build-dependencies]
bindgen = "0.72.1"

[dependencies]
hal = { version = "0.1.0", path = "../hal" }
log = "0.4.33"
